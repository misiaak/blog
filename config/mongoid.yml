development:
  sessions:
    default:
      database: project_manager_development
      hosts:
        - localhost:27017
      options:
  options:
production:
  sessions:
    default:
      uri: <%= ENV['MONGOLAB_URI'] %>
  